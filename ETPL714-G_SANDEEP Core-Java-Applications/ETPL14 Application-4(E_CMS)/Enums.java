package Package.com.evergent.coreJava.sandeep.application4;

enum module1 {
	S, P, R, E
};

enum submodule1 {
	A, F
};

enum submodule2 {
	T, P, M
};

enum submodule3 {
	I, S, M
};

enum Products {
	MOBILES, LAPTOPS
}

enum Mobiles {
	REALME, SAMSUNG, REDMI
};

enum Laptops {
	DELL, HP, LENAVO
}

enum Payment {
	CREDITCARD, DEBITCARD
}